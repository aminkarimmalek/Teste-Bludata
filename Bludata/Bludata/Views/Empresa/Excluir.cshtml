@model MySqlASPNetMVC.Models.Empresa
@{
    ViewBag.Title = "Excluir esta Empresa?";
}

<div class="page-header">
    <h1>Empresa <small>organize o cadastro de empresas</small></h1>
    <a href="@Url.Action("Cadastrar","Home")" class="btn btn-sm btn-primary">Nova Empresa</a>
</div>
<div class="alert alert-danger">
    <strong>Atenção!</strong> Deseja realmente excluir este registro?
</div>
<fieldset>
    <legend>Detalhes da Empresa</legend>
    <div class="form-group">
        <strong>Nome: </strong> @Model.NOME_FANTASIA
    </div>
</fieldset>

<hr />
<form method="POST" action="">
    <input type="hidden" value="@Model.Id" name="Id" id="Id" />
    <p>
        <button type="submit" class="btn btn-sm btn-info">Excluir</button>
        <a href="@Url.Action("Index","Home")">Cancelar</a>
    </p>
</form>